<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/slf_img_add_attach"
    android:layout_width="60dp"
    android:layout_height="60dp"
    android:layout_margin="5dp"
    android:background="@drawable/slf_feedback_add_photo_bg"
    >

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/slf_center_addimg"
            android:layout_width="17dp"
            android:layout_height="17dp"
            android:layout_above="@id/slf_photo_count"
            android:layout_centerHorizontal="true"
            android:src="@drawable/slf_photo_add_img" />

        <TextView
            android:id="@+id/slf_photo_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:layout_marginTop="3dp"
            android:gravity="center"
            android:text="@string/slf_feedback_photo_count"
            android:textColor="@color/slf_feedback_page_child_text"
            android:textSize="12sp" />
    </LinearLayout>
</RelativeLayout>